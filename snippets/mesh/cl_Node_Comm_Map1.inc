// Access first entry in node communcation map. should be 0.
moris::size_t ncmap1_node0 = ncmap1.node_lid(0);

// Access fifth entry in node communcation map. should be 3.
moris::size_t ncmap1_node5 = ncmap1.node_lid(5);
