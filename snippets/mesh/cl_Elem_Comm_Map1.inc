// Access first entry in Element LIDs. Should be 0.
moris::size_t ecmap1_elem0 = ecmap1.elem_lid(0);

// Access sixth entry in Element LIDs. Should be 3.
moris::size_t ecmap1_elem5 = ecmap1.elem_lid(5);
