The following files generate the binary files that are needed for the tests.

* save_matrix_to_binary :
  saves a MATLAB matrix into a binary file that can be read by MORIS

* integrate_poly_quad.m:
  generates and integrates a polynomial, solution to be testet with integrator

* integrate_poly_hex.m:
  generates and integrates a polynomial, solution to be testet with integrator
