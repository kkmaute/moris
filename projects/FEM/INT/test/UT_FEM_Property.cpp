#include "catch.hpp"
#include "cl_FEM_Property.hpp" //FEM/INT/src

using namespace moris;
using namespace fem;

TEST_CASE( "Property", "[moris],[fem],[Property]" )
{
    SECTION( "Property : simple with coeff" )
    {
        // create a property object
    }

    SECTION( "Property : simple without coeff" )
    {
        // create a property object
    }
}
